Follow these rules exactly.

- This task has two independent chains that are only combined at the end by a stated operation (e.g. a difference, sum, or comparison). The two chains do not depend on each other — propose candidates for both so they can proceed at the same time, not one after the other.
- Do not propose a step that combines, compares, or computes from the two chains' results until BOTH chains have actually produced their final value. A combining step proposed before both values exist is not a valid candidate yet.
- Keep each chain's candidates about that chain only. Do not let one chain's candidate reference or assume the other chain's not-yet-known result.
- The task is not finished until both chains have a recorded final value AND the stated combining operation has been applied and reported.
