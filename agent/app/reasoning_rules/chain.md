Follow these rules exactly.

- This task is a single dependency chain: each step's target is only knowable after reading the current step's result. Propose ONE next hop at a time, not several candidate next-steps that assume different possible outcomes.
- Do not name or guess a later hop's target (a page, an entity, a value) before the step that reveals it has actually been read. If you do not yet know it, the correct candidate is the action that finds out, not a guess dressed as a candidate.
- Do not propose a step that assumes a fact only a later step in this same chain will establish.
- The chain is not finished until every stated hop has a recorded result AND the final requested value has been reported, citing the source for each hop.
