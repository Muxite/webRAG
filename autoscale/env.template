# Environment Variables Template
# Copy this to .env (at project root) and fill in your values
# Note: AWS configuration should go in aws.env (see aws.env.template)

# Service URLs (for local development - build script converts for AWS)
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
REDIS_URL=redis://redis:6379
CHROMA_URL=http://chroma:8000

# LLM Configuration
MODEL_API_URL=https://api.openai.com/v1
MODEL_NAME=gpt-4

# Search API (move to keys.env if secret)
SEARCH_API_KEY=your-search-api-key-here

# Supabase Configuration (move secrets to keys.env)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
SUPABASE_ALLOW_UNCONFIRMED=false

# Agent Configuration
AGENT_INPUT_QUEUE=agent.mandates
AGENT_STATUS_TIME=10
AGENT_FREE_TIMEOUT_SECONDS=300

# Worker Status (Redis)
WORKER_STATUS_SET_KEY=workers:status
WORKER_STATUS_TTL=60

# Retry Configuration
DEFAULT_DELAY=2
DEFAULT_TIMEOUT=5
JITTER_SECONDS=0.5

# Quota Configuration
DAILY_TICK_LIMIT=1000

# Variables NOT needed in .env (handled by build script or aws.env):
# - AWS_REGION, ECS_CLUSTER, ECS_ENABLED → Set by build script or aws.env
# - GATEWAY_URL → Only for local CLI, not containers
# - QUEUE_NAME, METRICS_INTERVAL, CLOUDWATCH_NAMESPACE → Was for metrics container (removed)
# - AGENT_STATUS_QUEUE → Only used in tests, production uses Redis
# - SERVICE_DISCOVERY_NAMESPACE, GATEWAY_SERVICE_NAME → In aws.env

